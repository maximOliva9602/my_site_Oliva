node_modules/
.env
.DS_Store
*.log
референси/
photo/
# blog posts зберігаються через Railway Volume, не в git
data/posts.json

# Локальна база CRM (SQLite) — на Railway лежить на Volume, не в git
data/*.db
data/*.db-wal
data/*.db-shm
